1) сначала доделать лексер и конвертер в ассемблер. использовать насм для сборки ассемблера
   -отработка всех лексем
   -отработка всех токенов аст
   -вывод целых чисел и переменных(по return и echo для проверки)
   -преобразование типов

2) как будет готов лексер и все слова, после покрытия тестами на насм - делать уже самописный
   ассемблер
