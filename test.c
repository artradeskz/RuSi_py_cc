#include lib.c

цел запуск() {
    печать(1, "привет мир\n", 21);
    
    // Добавить вызовы других функций
    печать_цел(-42);
    печать_цел(100);
    
    char str[] = "hello";
    длина_строки(str);
    
    // Или через системные вызовы
    sys_write(1, "test\n", 5);
    
    вернуть 0;
}

/*
цел запуск() {
    печать(1, "привет мир\n", 21);
    вернуть 0;
}
*/
